#include <iostream>
using namespace std;

int main() {
    // 여기에 코드를 작성해주세요.
    string first;
    string second;
    cin >> first >> second;
    cout << first.length() + second.length();
    return 0;
}