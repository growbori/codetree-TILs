#include <iostream>
using namespace std;

int main() {
    // 여기에 코드를 작성해주세요.
    int a, b, c, d, e, f;
    cin >> a >> b >> c >> d >> e;
    f = (a> b) ? 1 : 0;
    cout  << f << endl;
    f = (a > c) ? 1 : 0;
    cout  << f << endl;
    f = (a > d) ? 1 : 0;
    cout  << f << endl;
    f = (a > e) ? 1 : 0;
    cout  << f << endl;
    return 0;
}